#!/usr/bin/env bash

# List all node_modules
alias all_node="find . -name "node_modules" -type d -prune -print | xargs du -chs"

# Delete all node_modules
alias del_node="find . -name 'node_modules' -type d -prune -print -exec rm -rf '{}' \;"
